import 'package:flutter/foundation.dart';

/// Dev mode is only active in debug builds. In release/profile builds this is
/// always false, which disables the dev menu, debug routes, and auth bypass.
const bool kDevMode = true;//kDebugMode;
